# Terraform Aliyun Scaffold (placeholder)

This directory is reserved for Aliyun deployment templates to mirror the AWS setup (VPC, load balancer, container services, secrets). It keeps the structure flexible for future multi-cloud support.

